# TODO - LIST
1. Move Contract Constructor types to a single file at types.ts in contract branch
    -   Make sure to replace Cover and Range constructors with these generic types
2. Convert Cover types to prefix with Cover to avoid type collisions