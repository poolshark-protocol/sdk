

describe('CoverPool tests', () => {
  test('should create CoverPool contract', () => {
    // expect(new CoverPool()).toStrictEqual(JSBI.BigInt(ethers.utils.parseUnits('5', 37).toString()));
  });
});